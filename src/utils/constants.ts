import { trailingSlashIt } from '@utils/helpers';

export const apiBase = trailingSlashIt(API_BASE);
export const appTitle = APP_TITLE;
export const appDescription = APP_DESCRIPTION;
export const registerSw = REGISTER_SW;
